<%= form_for(@app_idea) do |f| %>
  <% if @app_idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_idea.errors.count, "error") %> prohibited this app_idea from being saved:</h2>

      <ul>
      <% @app_idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tweet %><br>
    <%= f.text_field :tweet %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
